{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker build docparser:python",
            "type": "shell",
            "command": "docker build . -t docparser:python",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}